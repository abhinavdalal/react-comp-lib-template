import <%= name %> from './<%= name %>';
import { <%= name %>Props, <%= name %>StylingProps } from './<%= name %>.types';

export type {
  <%= name %>Props,
  <%= name %>StylingProps,
};

export {
  <%= name %>,
};

export default <%= name %>;
