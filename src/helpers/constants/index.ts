import variables from './main.module.scss';

export const SHORT_TRANSITION_DURATION = Number(variables.shortTransitionDuration.slice(0, -2));
export const LONG_TRANSITION_DURATION = Number(variables.longTransitionDuration.slice(0, -2));
